---
name: Build and Publish Docker Image
on:
  push:
    tags:
      - v*
  workflow_dispatch: null
permissions:
  contents: read
  packages: write
jobs:
  build-test-and-publish:
    name: Build, Test, and Publish Multi-arch Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up QEMU for multi-arch builds
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/kb-dashboard-compiler
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}
      - name: Build Docker image for testing
        uses: docker/build-push-action@v6
        with:
          context: packages/kb-dashboard-compiler
          platforms: linux/amd64
          load: true
          tags: kb-dashboard-compiler:test
          cache-from: type=gha
      - name: Run smoke tests
        shell: bash
        run: IMAGE_NAME="kb-dashboard-compiler:test" make compiler test-docker-smoke
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push multi-arch Docker image
        uses: docker/build-push-action@v6
        with:
          context: packages/kb-dashboard-compiler
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
