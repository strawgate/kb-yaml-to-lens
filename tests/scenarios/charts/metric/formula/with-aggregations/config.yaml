dashboards:
-
  name: formula-with-aggregations
  description: "A dashboard with a formula using multiple aggregation functions"
  panels:
  - title: "Performance Variance"
    id: hkiippp-pdmi-emn8-kenj-mpqikkkko # Fixed ID for testing
    type: charts
    grid: { x: 0, y: 0, w: 24, h: 15 }
    chart:
      type: metric
      id: omh6bho-qdo0-fnnn-jooooom-je4ph71mk8h # Fixed ID for testing
      data_view: metrics-*
      primary:
        formula: "(max(field='response.time') - min(field='response.time')) / average(field='response.time')"
        id: ljcenog-pp4q-fjpq-mhpl-mmfkl84pkjd3 # Fixed ID for testing
        label: "Variance"
