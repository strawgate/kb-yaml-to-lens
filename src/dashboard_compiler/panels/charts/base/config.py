from dashboard_compiler.shared.config import BaseIdentifiableComponent


class BaseChart(BaseIdentifiableComponent):
    """Represents a base chart configuration."""

    # data_view: str = Field(default=...)
