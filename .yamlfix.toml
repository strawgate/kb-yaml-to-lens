# yamlfix configuration
# https://lyz-code.github.io/yamlfix/

line_length = 140
sequence_style = "block_style"
none_representation = "null"

# Note: exclude patterns in this file are not reliably honored by yamlfix CLI
# Exclusions are handled via explicit directory listing in Makefile instead
