{
  "install": "curl -LsSf https://astral.sh/uv/install.sh | sh && source $HOME/.local/bin/env && npm install -g markdownlint-cli && make all install",
  "start": "source $HOME/.local/bin/env",
  "terminals": []
}
